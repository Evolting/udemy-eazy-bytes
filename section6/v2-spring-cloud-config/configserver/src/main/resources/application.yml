spring:
  application:
    name: "configserver"
  profiles:
    active: git
  #    active: native
  cloud:
    config:
      server:
#        native:
#          search-locations: "classpath:/config"
        git:
          uri: https://github.com/eazybytes/eazybytes-config
          default-label: main
          timeout: 5
          clone-on-start: true
          force-pull: true
  rabbitmq:
    host: localhost
    port: 5672
    username: "guest"
    password: "guest"

encrypt:
  key: "22366E87F5BF8F99"


server:
  port: 8071
